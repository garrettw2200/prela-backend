[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m app.main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# No health check for background workers
